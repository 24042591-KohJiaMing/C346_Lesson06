import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        paddingVertical: 40,
        marginHorizontal: 15,
        alignItems: 'center',

    },

    header: {
        fontSize: 20,
        fontWeight: 'bold',
        margin: 2,
        color: '#222',
        textAlign: 'center',
    },

    question: {
        fontSize: 18,
        fontWeight: 'bold',
        marginTop: 8,
    },
});
